apiVersion: v1
kind: ServiceAccount
metadata:
  name: health-system
  namespace: health-system
  labels:
    app.kubernetes.io/name: health-system
  annotations:
    # This annotation links to the IAM role for service accounts (IRSA)
    # Will be patched by environment overlays with the actual role ARN
    eks.amazonaws.com/role-arn: ""
