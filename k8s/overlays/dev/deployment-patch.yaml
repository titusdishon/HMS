apiVersion: apps/v1
kind: Deployment
metadata:
  name: health-system
spec:
  replicas: 1
  template:
    spec:
      containers:
        - name: health-system
          resources:
            requests:
              memory: "256Mi"
              cpu: "100m"
            limits:
              memory: "512Mi"
              cpu: "500m"
